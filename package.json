{
  "name": "todo",
  "version": "1.0.0",
  "description": "todo sample with firebase, vuex",
  "author": "shuent",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon src/server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node src/server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/bulma": "^1.2.0",
    "@nuxtjs/dotenv": "^1.3.0",
    "cross-env": "^5.2.0",
    "express": "^4.16.3",
    "firebase": "^5.6.0",
    "nuxt": "^2.0.0",
    "vuex-map-fields": "^1.3.1"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
